// The prerender build launcher is packages/studio/scripts/prerender-build.mjs —
// plain ESM that imports from the compiled dist/ after `npm run build`.
// This file exists only to satisfy editors that expect a .ts companion; it
// contains no runnable code.
export {};
