/**
 * Token-variant adapters: degenerate adapters that reuse the sketch adapter's markup
 * and change only token values.
 *
 * Placeholder until story 4.1. Knows nothing about which journey it is rendering — see AGENTS.md.
 */
export const PACKAGE_NAME = '@design-space/adapter-tokens';
