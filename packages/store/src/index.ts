/**
 * The store: the (object, ref) resolver. Git-backed in phase 1.
 *
 * Placeholder until story 1.2. Nothing above this resolver may construct a path — see AGENTS.md.
 */
export const PACKAGE_NAME = '@design-space/store';
