/**
 * The pipeline: plan / gate / layer / execute over @verevoir/recipes/engine.
 *
 * Placeholder until story 5.2. Drives the engine; does not run it — see architecture §8.
 */
export const PACKAGE_NAME = '@design-space/pipeline';
