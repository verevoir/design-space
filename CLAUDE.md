# design-space

The canonical agent context for this repository lives in **AGENTS.md** — imported below. This
file keeps **no copy of its own**: a single source of truth, per the
`agent-context-file-maintained` practice.

@AGENTS.md
